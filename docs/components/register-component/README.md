# RegisterComponent

- Позволяет выбрать тип пользователя: `user` (email) или `mop` (код из 5 цифр)
- Валидирует обязательные поля, код агента (ровно 5 цифр) и подтверждение пароля
- Использует `AuthService` для сохранения пользователя и автоматического входа
