# Быстрый старт

## Шаг 1: Установка зависимостей

### 1.1 Установка зависимостей фронтенда (Angular)

**Откройте терминал в корне проекта** (папка `simple-app-test`) и выполните:
```bash
npm install
```
Это установит все зависимости для Angular приложения.

### 1.2 Установка зависимостей бэкенда (Node.js)

**В том же терминале** перейдите в папку server и установите зависимости:
```bash
cd server
npm install
cd ..
```

Или можно выполнить в отдельном терминале, но обязательно из папки `server`:
```bash
cd server
npm install
```

## Шаг 2: Запуск приложения

### Важно: нужны ДВА терминала!

### Терминал 1 - Запуск бэкенда

**Откройте первый терминал** и выполните:
```bash
cd server
npm start
```

Вы должны увидеть:
```
🚀 Сервер запущен на http://localhost:3000
📝 API доступен по адресу http://localhost:3000/api
💡 Для остановки нажмите Ctrl+C
```

**Оставьте этот терминал открытым!** Сервер должен продолжать работать.

### Терминал 2 - Запуск фронтенда

**Откройте второй терминал** (новый!) и выполните:
```bash
npm start
```

**Важно:** Запускайте из корня проекта (папка `simple-app-test`), не из папки `server`!

Вы должны увидеть что-то вроде:
```
** Angular Live Development Server is listening on localhost:4200 **
```

Откройте браузер: http://localhost:4200

### Альтернатива: Автоматическая перезагрузка бэкенда

Если хотите, чтобы бэкенд автоматически перезагружался при изменениях, используйте:
```bash
cd server
npm run dev
```
(вместо `npm start`)

## Шаг 3: Проверка работы

### 3.1 Проверка бэкенда

Откройте в браузере: http://localhost:3000/api/health

Должен вернуться JSON:
```json
{"message": "Сервер работает!", "status": "ok"}
```

### 3.2 Тестирование приложения

1. Откройте http://localhost:4200
2. Нажмите "Регистрация"
3. Заполните форму:
   - Имя: любое имя
   - Email: ваш email
   - Пароль: минимум 6 символов
   - Подтверждение пароля: тот же пароль
4. Нажмите "Создать аккаунт"
5. После успешной регистрации вы автоматически войдете в систему
6. Проверьте, что ваше имя отображается в навигации справа
7. Нажмите "Выйти" для выхода
8. Войдите снова, используя те же данные

## Остановка приложения

В каждом терминале нажмите `Ctrl+C`

**Важно:** Сначала остановите фронтенд, потом бэкенд (или наоборот, не важно, но остановите оба!)

## Устранение проблем

### Проблема: "npm install" не работает

**Решение:**
- Убедитесь, что вы в правильной папке
- Для фронтенда: должна быть папка `src/` и файл `package.json` в корне
- Для бэкенда: должна быть папка `server/` с файлом `package.json` внутри

### Проблема: Порт 3000 уже занят

**Решение:**
Измените порт в файле `server/server.js`:
```javascript
const PORT = 3001; // вместо 3000
```
И обновите URL в `src/app/services/auth.service.ts`:
```typescript
private readonly API_URL = 'http://localhost:3001/api/auth'; // вместо 3000
```

### Проблема: Порт 4200 уже занят

**Решение:**
Angular CLI автоматически предложит использовать другой порт (например, 4201). Используйте тот порт, который предложит CLI.

### Проблема: Ошибка CORS

**Решение:**
- Убедитесь, что бэкенд запущен на порту 3000
- Проверьте, что в `server/server.js` есть строка: `app.use(cors());`
- Убедитесь, что фронтенд обращается к правильному URL

### Проблема: Не работает авторизация

**Решение:**
1. Проверьте, что бэкенд запущен (откройте http://localhost:3000/api/health)
2. Откройте консоль браузера (F12 → Console)
3. Проверьте вкладку Network (F12 → Network) на наличие ошибок
4. Убедитесь, что оба сервера работают одновременно

### Проблема: "Cannot find module" при запуске бэкенда

**Решение:**
Вы забыли установить зависимости бэкенда:
```bash
cd server
npm install
```

### Проблема: Angular не запускается

**Решение:**
Вы забыли установить зависимости фронтенда:
```bash
npm install
```
(из корня проекта, не из папки server!)

## Структура команд по папкам

```
Корень проекта (simple-app-test/)
├── npm install          ← Установка зависимостей фронтенда
├── npm start            ← Запуск фронтенда
└── server/
    ├── cd server
    ├── npm install      ← Установка зависимостей бэкенда
    └── npm start        ← Запуск бэкенда
```
