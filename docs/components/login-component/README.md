# LoginComponent

- Поддерживает два типа пользователей: `user` (email) и `mop` (код из 5 цифр)
- Для `mop` отображает отдельную кнопку «Вход через Auth0» (пока-заглушка)
- Использует `AuthService` для отправки запросов на бэкенд
- После успешного входа показывает имя пользователя в навигации
